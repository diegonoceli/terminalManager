# Quickstart

This feature does not change how the app is started. Run `npm start` as usual.

To test the features:
1. Open multiple terminals and move them around.
2. Scroll over the terminals (wheel). The canvas should NOT zoom out.
3. Close the application.
4. Restart the application. The terminals should appear exactly where they were.
